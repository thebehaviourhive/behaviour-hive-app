@AGENTS.md

# Supabase / Postgres gotchas

Learned the hard way, on the School Incident Log build. Read before writing any code that touches RLS.

- **RLS on UPDATE silently filters, it doesn't error.** A `.update()` whose target row fails the policy's USING clause returns `{ data: [], error: null }` — no thrown error, no signal at all. Never trust the absence of a client-visible error as proof a write succeeded, or that a negative test correctly blocked something. Re-read the actual persisted state afterward via a privileged (service-role) query.

- **A WRITE BEFORE A RAISE DOES NOT SURVIVE.** An uncaught exception rolls back the whole transaction the RPC runs in, including any insert or update earlier in the same function. `redeem_passport_claim_code()` recorded a rate-limit failure and then raised — so the limiter recorded nothing at all from the day it shipped. If a function needs to record something AND refuse, it must return rather than raise, or record in a separate transaction. This is not obvious from reading the function — it reads as though the insert lands and then the caller errors. Found by a targeted one-sign-in diagnostic (12 wrong-code attempts, `code_lookup_attempts` stayed at 0 rows every time), not by code review; fixed in migration 0116 by matching `lookup_passport_by_code()`'s own original pattern (0034) — `insert ...; return;` for the case that needs to record and fail, a normal zero-row return instead of an exception.

  The rule alone over-triggers — a schema-wide audit immediately after this fix found three OTHER functions with the identical textual shape (write, then later a raise) that are correctly written, not the same bug: `send_message()`, `create_incident_stamp()`, and `generate_passport_claim_code()`. In each, the write is part of the entity being atomically constructed — a message, an incident stamp, a code reissue — and rolling it back on failure (no valid recipients, an unlinked child, ten random-code collisions) is the correct behavior, not a silent loss. The actual bug class is narrower: a write to a table whose entire JOB is to survive the call's own failure — an audit log, an attempt counter, a rate limiter. `code_lookup_attempts` is currently the only such table in this schema. Audit any function that writes then raises against that distinction specifically, not the bare textual pattern — without it, the next audit produces false alarms and someone "fixes" correct atomicity.

- **Chaining `.select()` onto `.insert()` requires the new row to pass the table's SELECT policy WITHIN THE SAME STATEMENT** (PostgREST sends `Prefer: return=representation`). If a row's visibility depends on related rows that don't exist yet in that same transaction — e.g. an incident whose visibility partly depends on its children, inserted a moment later — the chained `.select()` can fail even though the bare insert, and a separate follow-up `.select()` afterward, both succeed. Don't work around this with a client-generated id (`crypto.randomUUID()`) to dodge the RETURNING clause; if the row's own existence should never be structurally possible without its children, fix it properly with a single atomic SECURITY DEFINER RPC that inserts everything in one transaction.

- **`.upsert()` TRIGGERS THE SELECT POLICY IMPLICITLY.** supabase-js sends `Prefer: return=representation` on `.upsert()` by default even with no `.select()` chained, so the new row must pass the SELECT policy within the same statement. Gotcha #3 above describes this for `.select()` chained onto `.insert()`; `.upsert()` does it invisibly, with nothing in the source to make it obvious. 0117 rewrote `passports`' SELECT policy to `owns_passport()` and silently broke every first-time passport creation, because the guardian row a trigger creates after the insert is not visible to the representation check in the same statement — live in production from the moment 0117 shipped until PRD 3 Stage 1 found it by accident, debugging an unrelated check. Not a claimed-guardian edge case: every brand-new parent's very first save, on both `/passport/welcome` and `/passport/section-a`. When changing ANY table's SELECT policy, grep for `.upsert()` against that table.

- **A FIXTURE THAT REUSES AN ACCOUNT TESTS THE SECOND CALL, NOT THE FIRST.** When a policy or check depends on a row a trigger creates in the same statement, the first-ever invocation is a different code path — visibility depends on something that does not exist yet. 0117 rewrote `passports`' SELECT policy to `owns_passport()` and broke every first-time passport creation for weeks, invisibly, because every fixture already had a passport row. When a policy references a trigger-populated row, that function's first invocation needs its own dedicated fixture. Not a blanket instruction to use fresh accounts — the precondition is the trigger dependency.

- **HOME LOGS REACH THE CLASSROOM BY DESIGN, VIA THE CLINICIAN.** A parent's home ABC entry is invisible to school staff. A clinician with active access reads the full home history, and `approve_fba_strategies()` publishes what they choose into `passport_clinical_content` as `strategy_school` / `strategy_shared` / `trigger` / `setting_event` — all school-visible. `strategy_home` is deliberately excluded. The raw log never crosses; the clinical interpretation does. This is shipped and working. Do not treat the home/school boundary as a dead end and build a route around it.

- **An embedded PostgREST join across a table with its own RLS returns EMPTY on the joined side, not an error.** `.select("incident_children(child_index, passports(child_name))")` silently drops the `passports` fields if the caller's session doesn't satisfy `passports`' own SELECT policy — even when the outer row (`incident_children`) is fully visible. The page renders wrong data (e.g. a name silently becomes "Unnamed child") with nothing failing anywhere to notice. When a relationship is deliberately broader than the joined table's own RLS — e.g. institution-roster-wide visibility that doesn't require the ordinary per-user grant (`passport_access`) — resolve it through a dedicated SECURITY DEFINER RPC instead of an embedded join. In this codebase: roster-scoped child names always resolve through `get_institution_child_roster()`, never a direct or embedded `passports(...)` read.

- **Whenever a migration changes behavior that existing adversarial checks depend on, re-audit those checks in the same session — and say so in the report.** A check can quietly become a no-op instead of failing: e.g. `get_parent_incidents()`'s gate changed from `status <> 'draft'` to `teacher_signed_at is not null`, which correctly made an existing check's row count go from 1 to 0 — but a *second* assertion right next to it read `row = rows?.[0]` and asserted a property was absent on that (now-undefined) row, which is vacuously true whether or not the property would actually leak. Nothing failed, so nothing flagged it; it was only caught by deliberately grepping for every place the changed function/table/column was referenced and reading each call site against the new behavior. Do that grep as a standing step after any migration that changes an existing function's gate, a table's shape, or a trigger's condition — not just when something conveniently fails first.

- **WHEN A FUNCTION'S RETURN CONTRACT CHANGES, UPDATE ITS CALLERS IN THE SAME PASS.** `0113` changed `get_child_clinical_document_status()` to always return one row with an `is_authorized` flag, instead of zero rows for "no document yet." Its own comment warned future callers to check `is_authorized` explicitly; the two EXISTING callers (`ClinicalSupportSection.tsx`, `CalmUnlockSheet.tsx`) were never updated, and every FBA-less passport showed "FBA complete" linking to `/passport/fba/null` — live from the moment 0113 shipped until Stage 5 Step 3 found it by chance, walking a genuinely fresh passport end-to-end. A comment protects readers, not code. Changing a `RETURNS TABLE` shape means grepping every consumer in the same migration, not just documenting the new contract for whoever writes the next one.

- **READ THE LIVE DEFINITION, NOT THE FIRST ONE YOU FIND.** A policy or function may have been altered several times. `0069`'s incident-edit policy genuinely lacked a `deactivated_at` check; `0105`/`0106`/`0107` superseded it and the current definition is correct. Flagging the historical version as a live gap wastes a fix and, worse, could produce one that reverts a later correction. Always resolve to the highest-numbered definition before concluding anything.

- **NINE INDEPENDENT LINEAGES CHECK `institution_staff` MEMBERSHIP, plus ~20 hand-written raw checks in individual RPCs.** The correct ones are correct by accumulated diligence, not by any enforced mechanism — `institution_staff_has_current_standing()` (0105) exists for exactly this and was used in one place until `0119`. Any NEW code checking `institution_staff` membership calls that helper. Never hand-write the `deactivated_at`/`approved_at` conditions again — it has been got wrong three times, including twice on the same policy across two dedicated fix passes (`passport_institution_links`' teacher-view policy, 0035 and 0112, neither of which was reading for this).

- **A COLUMN NAME IS A CLAIM. IF NOTHING WRITES IT TRUE FOR THE REASON ITS NAME SAYS, THE CLAIM IS FALSE.** `passport_institution_links.approved_by_parent` was a genuine consent record when it had exactly one writer: a parent, approving a school by code. PRD 3 Stage 2 removed that writer (`ShareBottomSheet`'s `handleApprove()`) as part of the school-led pivot, but `create_school_passport()` still sets it `true` on every school-created child — correctly, since a school creating its own child's record has no parent to seek approval from, and the flag is what keeps that link visible to gates written when it meant something else. The column is now a COMPATIBILITY DEFAULT, not a consent record, for every row it produces. Left un-commented, `approved_by_parent = true` on a school-created passport reads as evidence a parent approved something — which would be false, and is exactly the kind of thing that gets cited in a subject access request or a DPA review. Commented directly in `create_school_passport()` (0140) for this reason: harmless while everyone building on this schema knows why, dangerous the moment someone reads it at face value without that context. The general lesson: when a column's own name stops matching what actually causes it to be set, comment the write site, don't just remember it.

- **DEAD RLS POLICIES DON'T ANNOUNCE THEMSELVES — NAME THEM WHEN THE LAST CALLER GOES.** `passport_access`'s "Teachers can insert access for approved, matching institutions" and "Teachers can reactivate their own revoked access" (live definitions: `0100`) both gate on `passport_institution_links.approved_by_parent = true`. `AddChildSheet` was their only real caller — the principal-driven `grant_passport_access()` is `SECURITY DEFINER` and bypasses ordinary RLS on `passport_access` entirely, so it never touched these policies. Deleting `AddChildSheet` (PRD 3 Stage 2) makes both policies permanently unreachable by any real flow. Dead, not wrong — not urgent to remove, but nothing else in the schema will ever surface that fact; the next person auditing `approved_by_parent`'s remaining live dependents would otherwise have to work this out from scratch.

- **BEFORE TEARING DOWN ANY FIXTURE, GREP THE ADVERSARIAL SUITE FOR ITS INSTITUTION CODE AND ACCOUNT EMAILS.** A fixture that looks like leaked debris can still be a live dependency: `ZZFIXTURESTAGE1` was correctly identified as leaked test data and correctly torn down (`scripts/dev/teardown.mjs`) — but `CHECK W`'s own `W11c` referenced that exact institution as its one live example of a `'grandfathered'` `approval_source` value (the signature of `0101`'s one-time historical backfill, which no production path can ever reproduce), and nothing checked for that reference before deleting it. One command — `grep -rn "<institution_code>\|<email-prefix>" scripts/incident-log-test/adversarial-verify.mjs` — would have caught it before the deletion instead of after, in a full-suite run's failure list.

- **FIXTURES AND NEW LIFECYCLE STATE.** When a migration adds a state a production path is responsible for setting, existing fixtures WILL break — and that is correct. Fix them by driving the real production path (the RPC, as a real signed-in session), never by setting the column directly in a service-role insert. A fixture that sets such a column directly is testing the database's shape, not the system's behaviour. This happened for real when `0100_institution_join_approval.sql` made `class_teacher`/`sna` joins pending by default: every fixture across the suite that inserted staff via service role had encoded "row exists = active," correctly went false, and 353 checks failed loudly rather than passing against a state production no longer produces — the opposite outcome to the earlier `status: "awaiting_attestation"` incident (a fixture hand-setting a column production derives, which stayed green while masking a real bug through 180 checks), from the identical root cause. The fix in both cases is the same: call the real RPC (`approve_staff_join()`, as the principal's own session), never assign the column. One more thing worth naming from this repair pass: fixing ~6 fixture blocks this way exercised `approve_staff_join()`'s happy path repeatedly as a side effect, but not one existing check asserts on that RPC's *own* correctness (authorization, refusing a non-principal or wrong-institution caller, refusing double-approval) — every downstream check only needed the state it produces to be right, not the RPC's own behavior verified. That verification doesn't exist yet anywhere in the suite; a fixture-repair pass fixing fixtures is not the same claim as Step 2 writing real coverage for a migration's own new surface, and shouldn't be mistaken for it.

- **The adversarial suite tests the database, not the journey — it never renders or drives a UI.** Every check calls the Supabase client directly with a real JWT; none of them touch a React component, a redirect, or conditional rendering. A check whose NAME describes a user-facing outcome ("a deactivated person rejoining...") can pass while the actual page a person uses is broken, if the bug lives entirely in client-side routing/gating logic rather than in RLS/an RPC/a trigger — the class of thing this suite is built to catch. This happened for real: `join-institution/page.tsx`'s `checkExisting()` had no `deactivated_at` filter, silently redirecting a deactivated person back to their old dashboard instead of the join form, while CHECK V10's own direct-INSERT check kept passing, honestly proving the database would accept a legitimate rejoin — a true claim, just narrower than the check's name promised. Found only by hand, not by the suite. When a check's claim is about what a user would *experience* rather than what the database *permits*, write it against the CLIENT'S OWN QUERY SHAPE, file:line-matched to the component that gates the real journey — not a proxy for it, the literal same filter — so a regression in that client code fails the check the same way a regression in an RLS policy would.

- **COVERAGE GAPS ARE NOT WRONG-REASON PASSES.** A wrong-reason pass is a check that asserts the wrong thing. A coverage gap is a mechanism with no check at all, exercised only as a side effect of other fixtures. `approve_staff_join()` had six fixtures depending on its happy path and zero checks on its guards. When a new RPC is added, ask what asserts its REFUSALS — not whether anything uses it.

- **THE SUITE ENCODES OUR BELIEFS, NOT TRUTH.** Adversarial checks catch contradictions between code and spec. They cannot catch an error IN the spec — a check written from a wrong assumption will confirm the bug. `0105` tightened an edit policy per instruction; the accompanying check asserted the same wrong thing and passed. When a check is written from a stated requirement rather than from observed behaviour, ask what would have to be true for the requirement itself to be wrong.

- **WHEN ACCESS OR AUTHORITY IS GRANTED, TEST THE DESTINATION.** Two gaps found in one session: an ownership transfer whose recipient could not edit what they received (`AA-8` proved the transfer record but never asked whether the new owner could act on it — `can_own_incident()` never recognised a principal as eligible), and an SNA assignment with no route for the assignee to reach the child (`assign_sna_to_child()` has been live since Stage 2; `/sna/passports` only ever queried `passport_access` directly, never `has_sna_access()` or any of the other access sources — the suite proved the grant, nothing asked whether the person could get to it). In both cases the grant was correct and adversarially proven. Granting access and being able to USE it are separate claims, and the suite only ever tests the first — it never renders a route. For every new access source, ask which screen surfaces it, and prove that screen queries the right thing.

  **Two more instances of the identical shape, found live on the deployed app during Stage 3's own browser-verification pass, in the SAME session that had just fixed `/sna/passports`** — a reminder that fixing one occurrence doesn't inoculate the neighbouring surfaces: (1) `useSnaChildren.ts`'s merged list correctly resolved a covering SNA's class-wide roster, but did it by reading `class_children` directly — a table whose own SELECT policy (0104) is deliberately narrower than `has_sna_access()`, current class teacher or principal only. The list query returned nothing, RLS-silent, for exactly the covering SNA it was built for; fixed with a narrow SECURITY DEFINER RPC (`get_temporary_access_covered_children()`), not a widening of that policy. (2) The list itself, once fixed, linked into `/sna/passport/[passportId]` — whose OWN access guard had never been touched, still a direct `passport_access`-only read predating this stage, refusing the exact child the list had just shown as accessible ("we couldn't find this classroom profile"). Fixed by replacing that guard with a call to `has_sna_access()` itself, so the guard and the underlying RLS can never independently drift again. Generalised: a fix at one layer (the list's own query) does not imply the layers it reads from or links to were fixed too — each hop in "grant → list → detail → write" is a separate claim, and needs testing separately. A THIRD component from the same stage (`TemporaryAccessBanner`) turned out to have been built, working, and never imported anywhere at all — found only by trying to see it live, not by any code-level reasoning; a component's own correctness proves nothing about whether it was ever wired in.

  **A separate, unrelated tooling trap surfaced during the same pass, worth recording so it isn't mistaken for a real bug again**: Next.js's client-side Router Cache can keep serving a stale RSC payload for a route across repeated `location.href` reassignments and even explicit hard reloads WITHIN THE SAME BROWSER TAB, even after the underlying deploy has genuinely gone green (confirmed via response headers showing `x-vercel-cache: MISS`, and the served JS bundle itself containing the new code, searched for directly). It looked exactly like the fix hadn't shipped. Closing the tab and opening a fresh one cleared it. If a just-deployed fix doesn't seem to be taking effect in browser verification despite the deploy SHA checking out, try a genuinely fresh tab before concluding the fix is wrong.

- **QUERY LIVE STATE, NOT AN EVENT LOG, FOR ANYTHING ACTIONABLE.** `school_notices` is append-only with nothing keeping it in sync with the tables holding current state. `'attestation_withdrawn'` fires once and knows nothing about a later re-attestation; `'incident_parent_call'` knows nothing about `mark_parent_called()`. A dashboard built on notices shows resolved items as outstanding until someone clicks them away — a persistent flag standing in for a live fact. Notices are a record of what happened; they are not a source of what is currently true. One extra join, and it cannot drift. Decided during PRD 2 Stage 7's own dashboard recon, weighing `school_notices` against querying `incident_children`/`incident_attestations` directly for "restraints needing a parent call" and "withdrawn attestations institution-wide" — neither built on notices, both on live state.

# Verification environment

- **Browser verification runs against the DEPLOYED app** — `.claude/launch.json`'s `deployed` entry (`https://behaviour-hive-app.vercel.app`), not `local-dev`. A local dev-server check exercises real RLS against the real Supabase project, which is genuinely useful, but it does not prove the deployed build behaves the same — different bundle, different environment. **State which environment was actually used in every report, every time** — the same way the deployed commit SHA gets stated, not left implied. "Verified live" with no environment named is not a real claim; it's the gap that let browser verification run against `local-dev` for two straight days (Aug 25–27) while every report during that window said "verified live" and meant local. Caught only when asked directly, not by anything failing.

- **WHEN A BROWSER-TOOL CLICK ON A CTA "TIMES OUT" BUT INSTEAD HIGHLIGHTS THE BUTTON'S LABEL TEXT, IT'S TOOLING, NOT THE APP — AND IT HAS A FIX.** Hit twice in the same session before being named properly (Stage 3's own browser-verification pass) and wrongly written off both times as "the documented input-synthesis outage" that "resolves on its own" — it doesn't resolve on its own, and treating it as an unfixable environment flake meant one interaction (the ABC logger's reactive lapsed-access message) went unverified in the first version of a report that claimed the rest of the stage was checked live. What's actually happening: the click resolves to the text node inside the button rather than the button element itself (visibly, the label gets selected/highlighted, like a text drag). The fix, in order: (1) dispatch a real click on the button ELEMENT via `javascript_exec` — find it (`document.querySelectorAll('button')`, matched by text or a stable selector) and call `el.dispatchEvent(new MouseEvent('click', {bubbles: true, cancelable: true, view: window}))` — this reliably opens sheets/triggers handlers the `computer` tool's click could not; (2) if that's not available, aim the coordinate click at the button's edge padding, not the centre of its label, so no text node sits under the pointer. Try (1) first — it worked every time it was tried. Don't spend another session's budget re-diagnosing this as a fresh outage.

# Deferred work

- **`get_passport_clinicians()`'S FOUR CALLERS TREAT EVERY ROW AS CURRENTLY CONNECTED — A REVOKED CLINICIAN STILL COUNTS.** Found building migration 0128 (PRD 2, Stage 4's clinician-history RPC): the function itself only ever returns `is_active = true` rows, but none of its four existing client callers check `is_active` at all — they were written when a caller-side check was unnecessary because the function never returned anything else, and nobody revisited that when 0123 added revocation. This is a live correctness gap independent of PRD 2, not something Stage 4 introduced or fixed (a dedicated `get_passport_clinician_history()` was added instead of widening the shared function, specifically so existing callers wouldn't start seeing revoked rows silently — the opposite problem). The four call sites, each needing its own `is_active`-aware fix: `src/app/passport/dashboard/page.tsx` (parent's own Clinical Team card), `src/components/parent/ClinicalSupportSection.tsx`, `src/components/parent/calm/CalmUnlockSheet.tsx` — the sharpest version: this decides whether Calm Cards are unlocked, so a clinician who lost access could still be treated as the reason Calm Cards are available — and `src/app/passport/fba/[fbaId]/print/page.tsx` (clinician specialty lookup for a print header, lowest stakes of the four but same bug). Not fixed here — named so it isn't rediscovered from scratch.

- **SUPABASE AUTH RATE LIMIT RAISED FOR TESTING.** "Sign-ups and sign-ins" raised from the default 30 to 300 requests per 5 minutes per IP so the full adversarial suite can complete in one run (root cause of the suite dying at the same point on every attempt regardless of how long between retries, finally diagnosed 2026-08-30: ~90 real sign-ins plus ~122 `createUser` calls in one run against a 30-per-5-minute window — not a slow-clearing hourly bucket as first assumed). This weakens brute-force protection on sign-in. Acceptable while the app has one trial school and no public traffic. **MUST be returned to 30, or a considered value, before real families and staff use the product at scale. Do not let this ship silently.**

- **THE PASSPORT_GUARDIANS DUAL-WRITE TRIGGER (`sync_passport_guardian_from_user_id()`, migration 0113) IS A BRIDGE, NOT PERMANENT — DO NOT LET IT BECOME ONE BY ACCIDENT.** Stage 5 rewrote `owns_passport()` to check `passport_guardians` instead of `passports.user_id`, but every existing fixture in the adversarial suite (and the real signup flow) still creates a passport by setting `passports.user_id` directly — this trigger keeps `passport_guardians` in sync underneath that so none of it had to change (proven empirically: 643/643 pre-existing checks passed unchanged after the migration ran). Remove the trigger once ALL of the following are true: (a) every write path that establishes a guardian relationship — signup, the claim-code flow (Step 2, not yet built), any future "add a second guardian" flow — inserts into `passport_guardians` directly, not via `passports.user_id`; (b) no adversarial fixture sets `passports.user_id` as its way of establishing guardian state; they all insert into `passport_guardians` directly or drive the real RPC; (c) `passports.user_id` itself is either fully retired or kept purely as historical/display metadata that nothing authorizes against. Until then, every NEW write path built on top of Stage 5 should write `passport_guardians` directly and treat this trigger as a safety net for old callers, not the intended mechanism going forward. Related, still open: `passports.user_id`'s own `unique(user_id)` constraint was deliberately NOT dropped in 0113 (it's a live `onConflict` target for `passport/section-a` and `passport/welcome`'s upserts) — dropping it needs a coordinated client change first, and separately needs an actual product decision about how the welcome flow tells "resume my passport" apart from "start a second child's passport" once a parent can legitimately have both. `passport_section_b/c/d`'s equivalent constraints have the same live-`onConflict`-target problem; 0113 added `unique(passport_id)` alongside the old `unique(user_id)` without dropping it yet — Step 1b (mechanical, low-risk: switch `usePassportSection{B,C,D}.ts`'s three `onConflict` targets, then drop the old constraints) is still open.

- **STRUCTURAL-ONLY CHECKS, STAGE 1 — CONVERTED WITH STAGE 1C.** Four call sites/surfaces protected a state that was unreachable before `hand_over_principal()` existed — `can_countersign_incident()`'s principal branch, `incident_locations` add/edit principal branches, `mark_parent_called()`, and `/principal/dashboard`'s own redirect-away behavior for a deactivated principal (corrected from an earlier "pending/rejected overlay" description — that page never renders an overlay at all, pending or rejected; it redirects to `/teacher/join-institution`, which is the one place any such overlay lives). All four needed SOME real deactivated principal to exist, which a 'leaving' handover now produces (`deactivated_at` set on the predecessor's row, `role` column untouched, still `'principal'` — structurally identical to what `deactivate_institution_staff()` would have produced if it could ever target a principal). All four converted: the first three via CHECK X's own adversarial coverage (`can_countersign_incident()` called directly, both `incident_locations` policy branches — add and edit are separate policies, tested separately — and `mark_parent_called()` isolated from its creator/owner bypass by using an incident the deactivated principal didn't create); the fourth via live browser verification on the deployed app — a genuinely departed principal, signing in fresh, lands on the plain join form via `/principal/dashboard`'s existing redirect, not stuck, not shown a wrongful message.

- **TWO items are PERMANENTLY STRUCTURAL, not four as an earlier pass through this list claimed — corrected after re-checking, not assumed equivalent.**
  - `deactivate_institution_staff()`'s last-principal guard: its ALLOW branch requires two simultaneously active principals at one institution, which the one-principal index excludes by design and handover's atomicity preserves. Defense-in-depth against a future relaxation of that invariant, not owed a live test. (Considered and rejected: reusing this RPC as handover's own 'leaving' mechanism would make the guard reachable, but only by first loosening its separate self-deactivation check — not a trade worth making.)
  - `approve_staff_join()`/`reject_staff_join()`'s caller check together with `derive_staff_join_approval()`'s EXISTS clause — the claim that a pending or rejected principal doesn't count as active. Originally listed as converting with Stage 1c on the reasoning that handover would open "a genuine window where a second principal-role join could exist as pending or rejected." Checked empirically once `hand_over_principal()` actually existed, and that reasoning was wrong: handover is atomic specifically so the institution is never observably principal-less, even momentarily — the successor's new principal row is inserted, active, in the same transaction that closes the predecessor's, so `institution_staff_one_principal_per_institution` is satisfied by exactly one row continuously, before and after. A third party's ordinary principal-role join attempt immediately after a real handover still fails at the same unique index, confirmed live: `duplicate key value violates unique constraint`. This item needs the SAME two-simultaneous-principals state as the last-principal guard above, for the identical structural reason — it belongs on this list, not the conversion one.

- **SECOND-PRINCIPAL JOIN — DONE.** `src/app/teacher/join-institution/page.tsx`'s `friendlyJoinError()` now points at the real mechanism: "This school already has a principal. Ask them to hand over the role to you, or join as a class teacher instead." — no "contact support," deliberately (that's the abandoned-principal path below, not the ordinary one).

- **SNA-SUCCESSOR HANDOVER — CLOSED.** `hand_over_principal()`'s own SQL always treated `class_teacher` and `sna` successors identically — nothing role-specific in its guards — but only the `class_teacher` case had ever been clicked through live; deliberately deferred at the end of Stage 1b rather than reasoned about ("I'd rather it was clicked than reasoned about"). Closed via a manual browser pass on the deployed app: a real SNA successor, handed the principal role via a genuine `hand_over_principal()` call, signs in fresh and lands on `/principal/dashboard` as the new principal — not `/sna/passports`, not stuck, not shown a wrongful message.

- **CROSS-INSTITUTION TEMPORARY ACCESS — KNOWN LIMITATION, NOT SOLVED.** Client-side routing (`useRequireRole`/`getPostAuthRedirect`) is single-role-per-account: `app_metadata.role` is one claim, read to decide which screens a person can even reach, entirely separate from the database-level access `grant_temporary_access()` (Stage 3) actually grants. A brand-new account (no prior claim) works — they self-select 'sna' at `/role-select` and reach it. An existing same-institution SNA colleague works — they already claim 'sna' and already reach `/sna/passports`. A person whose account already claims a DIFFERENT role — most plausibly a class teacher at one school covering as supply staff at another — has no reachable path to the covering assignment at all, even though the database would grant it correctly. `grant_temporary_access()` deliberately does not override someone's own claimed identity to work around this — that would break their own primary navigation elsewhere. Not attempted: a multi-role-per-account redesign, which is real, separate, and out of this stage's scope. Worth knowing whether this bites in practice — supply teachers in Irish special schools plausibly work across several schools — and if common, belongs in PRD 2's scope discussion, not assumed rare.

- **CLINICIAN DOUBLE-ENGAGEMENT — KNOWN LIMITATION, NOT SOLVED.** `clinician_access`'s own `unique(passport_id, clinician_id)` constraint means the same clinician cannot be both parent-engaged and institution-engaged for the same child — there is exactly one row per (child, clinician) pair, and `engaged_by` can only ever hold one value at a time. A family's own private psychologist who is also the specific person a school wants to formally bring in has no way to hold both relationships at once in this schema. Real, but rare — solving it means a second row (or a join table) representing two independent authorities over one clinical relationship, meaningfully more complexity than the case has so far justified. What IS built, deliberately, so the gap is never silent: both `grant_clinician_access()` and `connect_clinician()` (0123) check for the OTHER authority's engagement before ever reaching the table's own unique constraint, and refuse with a specific, explanatory message — a principal trying to engage a parent-already-engaged clinician is told plainly why, never shown a raw `duplicate key value violates unique constraint`.

- **PRINCIPAL HANDOVER — ABANDONED CASE.** If a principal leaves without handing over, no in-product path exists to appoint a replacement. Proper fix is `institution_admin` onboarding (C-08, open since `0033`). Until then this is a documented Behaviour Hive support operation performed with the service role.

- **REAL BUG, NOT YET FIXED: `fetchApprovedInstitutionPhone()` ([src/lib/messages/institutionPhone.ts](src/lib/messages/institutionPhone.ts)) can show the wrong school's emergency contact number.** Its query is `passport_id` + `approved_by_parent = true`, `.limit(1)`, with no `institution_id` scoping at all. Nothing in this schema has ever stopped a child having approved links to two institutions at once (confirmed live, Stage 4 Step 1's CHECK DD, and Stage 6's one-active-enrolment constraint doesn't exist yet) — when that happens, this function returns *an* approved institution's phone number, not a defined one, and Postgres row order is not something to rely on. This is the compose sheet's tap-to-call fallback for someone trying to reach a child's school urgently, so the wrong number here isn't cosmetic. Not this stage's to fix — named here so it isn't lost as a check-name footnote (`DD-3d` records the underlying query behavior, not the fix). Real fix likely needs the caller's own institution context threaded through rather than a bare passport-level lookup — worth its own pass, not a drive-by change.

- **CLOSED, PRD 3 STAGE 1 + STAGE 3: a claimed passport can now be both READ and COMPLETED by its guardian.** Was: `passports`' SELECT policy moved to `owns_passport()` (0117) while INSERT/UPDATE stayed `user_id`-keyed, so a claimed guardian could see their child's passport but not fill it in — `section-a` landing them on a blank "create new" form that would silently make an unrelated second passport. Stage 1 extended `section-a`/`b`/`c`/`d`'s own write paths to guardian-based auth (`useMyPassport()`, explicit insert-vs-update, never a `user_id`-keyed upsert). Stage 3 closed the remaining gap — the write path worked but nothing surfaced the door in: `parent-dashboard`'s own "Build your child's passport" card was still gated on self-created-only, its `resumeHref` defaulting to a dead end for anyone claimed. Fixed to show for a claimed guardian too, pointing straight at `/passport/section-a`, and to stop rendering the moment `section_a_complete` is true.

- **FIXED, STAGE 5 STEP 3: fourteen parent-facing screens used to resolve "my passport" via `.from("passports").eq("user_id", user.id).maybeSingle()`, and `.maybeSingle()` throws if more than one row matches.** A parent with two children's passports would have hit a hard error on every one of these screens, not silently seen one child — worse than the guardian-visibility gap Stage 5 Step 2 fixed alongside it. This had never been observable because `passports.user_id unique` (still live; dropping it is the Step 1b work named above) has always made two rows structurally impossible — the bug was real and sitting underneath that constraint since before this stage existed, not introduced by it. `get_my_passports()` (Stage 5 Step 2) fixes this properly by returning a list instead of a single row; ten of the fourteen sites now go through it via the shared `useMyPassport` hook ([useMyPassport.ts](src/hooks/useMyPassport.ts)): `parent-dashboard/page.tsx`, `passport/dashboard/page.tsx`, `passport/progress/page.tsx`, `parent-dashboard/activity/page.tsx`, `parent-dashboard/incidents/page.tsx`, `messages/page.tsx`, `morning-checkin/page.tsx`, `more/page.tsx`, `CalmUnlockSheet.tsx`, `useParentCalmAccess.ts`. The remaining four — `passport/section-a/page.tsx` and `usePassportSectionB/C/D.ts` — are deliberately NOT migrated: they're one coherent unit, the self-created-passport wizard, and both their read AND their write are keyed on `user_id` (the section tables' own `onConflict: "user_id"` upsert, Step 1b's separate open item) — migrating only the read half would resolve a guardian's OWN claimed passport into a form whose write path would silently create an unrelated second passport row instead of editing the one just resolved. Left exactly as-is; the residual risk (a claimed-passport guardian navigating here directly by URL gets a blank "create new" form rather than an error) is real but safe — it can create an extra passport, never corrupt the claimed one — and is now genuinely contained by `passport/welcome/page.tsx`'s own redirect (added in the same pass): a parent who already has any passport is sent straight to `/passport/dashboard` on landing, never shown the create-new form at all. `passport/section-d/4/page.tsx`'s own passport UPDATE (`.eq("id", passportId)`) was already id-keyed, not part of this bug, no change needed. Also added this same pass, at `/passport/claim`: the claim screen itself (Requirement 4), and `/passport/welcome`'s "Already have a code from your child's school?" branch (Requirement 3) linking to it. `/principal/passports/[passportId]` (Requirement 2) now shows either the claim-code generation state or the guardian list, driven by `get_passport_claim_code_status()`/`get_passport_guardians_for_child()` (both 0114) — exactly the state `get_passport_guardians_for_child()`'s own migration comment named as missing ("the section vanishes is not a designed state"). One loose end from this: `revoke_passport_claim_code()` (0114/0115) still has no client caller anywhere — `get_passport_claim_code_status()` doesn't return the claim code's own `id`, only `code`/`expires_at`, so there's nothing for a standalone "Revoke" button to call. Not fixed here: the principal UI uses "Generate a new code" instead, which `generate_passport_claim_code()` already implements as an atomic revoke-then-reissue of the caller's own prior code — a real, working substitute for the common case, not a gap in what a principal can actually do, but `revoke_passport_claim_code()` itself is dead code today.

- **KNOWN LIMITATION, NOT SOLVED: two guardians of the same passport can each independently see "not checked in yet" and both submit a morning check-in for the same child on the same day.** Found via a proactive sweep (Stage 5 Step 3) checking whether `morning_checkins`' own RLS (raw `auth.uid() = user_id`, migration 0006) was the same inline-ownership-check bug 0118 fixed elsewhere — it isn't; `morning_checkins.user_id` correctly means "who personally submitted this," the same own-contributor semantics as `passport_section_b/c/d`, not a passport-ownership stand-in, so no RLS fix is needed. The real gap is one layer up, in `parent-dashboard/page.tsx`'s own query: `.eq("user_id", user.id)` answers "did *I* submit today," when the check-in card's actual intent ("let the teacher know") is passport-scoped, not user-scoped. Before Stage 5, this was unreachable (one guardian per passport, structurally). Now that `passport_guardians` makes a second guardian real (via the claim flow), it's a live, if minor, gap — worth a follow-up switching that query to `.eq("passport_id", passportId)` instead (RLS already permits the self-read either way; this is a pure client-query fix, no migration needed), not attempted in this pass.

# Parked work

- **PARENT INVITATION BY EMAIL — PARKED.** Claim codes must currently be handed over in person, which fails once a school is inviting parents remotely. Recon done. Scope when picked up: an email carrying the school's name, an explanation and a LINK to the claim screen — NOT the code and NOT the child's name, so a mistyped address costs nothing and the credential stays on a separate channel. Principal-only, one action beside "Generate code". The show-the-code path stays untouched.

  Open questions: whether Resend already exists as Supabase's custom SMTP provider (dashboard config, invisible to a codebase grep) and whether its domain is already verified — which would remove both the deliverability warm-up and the new-processor question. Do NOT use `inviteUserByEmail()`: it routes through the Supabase Auth signup bucket, the same one currently inflated 30→300 and flagged as must-revert-before-scale.

  Auto-applying the code through signup is a SEPARATE, larger piece of work — confirmation is OTP-entry not a magic link, so there is no link-click for a query param to ride on, and chaining it would touch shared registration code every account type depends on.

- **PARENT-TRACK LEFTOVERS FROM THE PARENT-LED MODEL — CLOSED, PRD 3 STAGE 2.** PRD 1 changed who owns a child's file; the parent's own screens went unrevisited until Stage 2. `AddChildSheet` (teacher/SNA self-link by passport code, gated on `approved_by_parent`) is deleted, along with its three call sites (`teacher/dashboard`, `teacher/students`, `sna/passports`) — recon found no gap, `grant_passport_access()` already covers every case it served and more. `ShareBottomSheet`'s `handleApprove()` (parent-approves-a-school-by-code) is deleted along with its entry point; the outbound passport-code section and clinician-connect are unrelated and untouched. The Connected Schools list stays as read-only information, relabeled, with the `approved_by_parent = true` filter dropped (it was hiding links the flag no longer distinguishes). Deployed `b555d49`, live-verified.

  Two copy locations promise a parent controls school access and still need to be found when Catherine's consent-copy rewrite lands, rather than rediscovered — this wasn't in Stage 2's scope, still open: [src/app/consent/page.tsx:60-65](src/app/consent/page.tsx) (onboarding consent card — "You control who sees it" / "No school sees anything without your active consent.") and [src/components/PrivacyPolicyContent.tsx:26-35](src/components/PrivacyPolicyContent.tsx) ("Who can see it" — "Access can be reviewed and revoked at any time from the passport's Manage Access section," which now names a removed feature by location).

